# // backend/Dockerfile
FROM node:20-alpine

# This creates the /app folder INSIDE the container
WORKDIR /app

# Copy package files first (for caching)
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the code
COPY . .

EXPOSE 4000

# Start with dev mode
CMD ["npm", "run", "dev"]