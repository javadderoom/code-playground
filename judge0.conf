# // judge0.conf
# Database settings
db_host = judge0-db
db_port = 5432
db_user = judge0
db_pass = judge0_password
db_name = judge0

# Redis settings
redis_host = judge0-redis
redis_port = 6379

# Network settings
# This allows Judge0 to talk to your backend if needed
allow_networking = true